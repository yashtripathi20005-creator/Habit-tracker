# Habit Tracker

A terminal-based habit tracking application with SQLite persistence.

## Quick Start

```bash
# Clone or create project directory
mkdir habit_tracker && cd habit_tracker

# Copy all files (main.py, database.py, models.py, habit_tracker.py, cli.py, config.py)

# Run
python main.py
